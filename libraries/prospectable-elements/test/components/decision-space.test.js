// import {
//   elementUpdated,
//   expect,
//   fixture,
//   html,
// } from '../../../../scripts/test';

// import '../../src/components/decision-space';

describe('decision-space', () => {
  it('HELP - CANNOT LOAD PLOTLY!');

  // it('has a shadowDom', async () => {
  //   const el = await fixture(html`<decision-space></decision-space>`);
  //   expect(el.shadowRoot).to.have.descendant('div.plotly');
  // });

  // it('has an empty lightDom', async () => {
  //   const el = await fixture(html`<decision-space></decision-space>`);
  //   expect(el).lightDom.to.equal('');
  // });
});
