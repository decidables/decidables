// import {
//   elementUpdated,
//   expect,
//   fixture,
//   html,
// } from '../../../../scripts/test';

// import '../../src/components/cpt-fit';

describe('cpt-fit', () => {
  it('HELP - CANNOT LOAD PLOTLY (OR, PROBABLY, WEB WORKER!');

  // it('has a shadowDom', async () => {
  //   const el = await fixture(html`<cpt-space></cpt-space>`);
  //   expect(el.shadowRoot).to.have.descendant('div.plotly');
  // });

  // it('has an empty lightDom', async () => {
  //   const el = await fixture(html`<cpt-space></cpt-space>`);
  //   expect(el).lightDom.to.equal('');
  // });
});
