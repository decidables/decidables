// import {
//   elementUpdated,
//   expect,
//   fixture,
//   html,
// } from '../../../../scripts/test-utility';

// import '../../src/components/ddm-fit';

describe('ddm-fit', () => {
  it('HELP - CANNOT LOAD WEB WORKER!');

  // it('has a shadowDom', async () => {
  //   const el = await fixture(html`<ddm-fit></ddm-fit>`);
  //   expect(el.shadowRoot).to.have.descendants('.param').with.length(2);
  // });

  // it('has an empty lightDom', async () => {
  //   const el = await fixture(html`<ddm-fit></ddm-fit>`);
  //   expect(el).lightDom.to.equal('');
  // });
});
